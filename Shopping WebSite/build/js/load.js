// create loader for site
window.addEventListener('load', function () {
    document.querySelector('.loading-container').classList.replace('flex', 'hidden')
    document.querySelector('.loading-page').classList.replace('hidden', 'flex');
});